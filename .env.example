# Backend environment
NODE_ENV=production
PORT=4000
JWT_SECRET=change_this_secret
# Comma-separated list of allowed browser origins, e.g. https://yourdomain.com
CLIENT_URL=https://mk.garo2.com,https://www.mk.garo2.com
FRONTEND_URL=https://mk.garo2.com
PASSWORD_RESET_TTL_MINUTES=30
RESEND_API_KEY=
RESEND_FROM_EMAIL=
# Optional: only set if this backend should also serve a built frontend folder
# FRONTEND_DIST=../frontend/dist
